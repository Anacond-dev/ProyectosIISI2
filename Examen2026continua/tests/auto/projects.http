
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8081/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing projects
### This query is used in several of the below tests it should be executed first.
# @name projects
GET {{BASE}}/projects

### Login a(n) users and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"username": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing projects by its id.
### positive test 
@proId = {{projects.response.body.0.projectId}}
GET {{BASE}}/projects/{{proId}} 
Content-Type: application/json 


### Test 02: Try get one existing projects by its nonexistent id.
### negative test 
GET {{BASE}}/projects/999999999
Content-Type: application/json 


### Test 03: Add a new projects successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newprojects
POST {{BASE}}/projects 
Content-Type: application/json 
Token: {{token}} 

{
	"userId": ###REPLACE###,
	"name": ###REPLACE###,
	"projectLeader": ###REPLACE###,
	"endDate": ###REPLACE###,
	"budget": ###REPLACE###,
	"description": ###REPLACE###,
	"imageUrl": ###REPLACE###
} 

### Check the created projects

@newprojectsid = {{newprojects.response.body.lastId}}
GET {{BASE}}/projects/{{newprojectsid}}
Content-Type: application/json 

### Test 04: Add a new projects without a session token
### Negative test 
POST {{BASE}}/projects 
Content-Type: application/json 

{ 
	"userId": ###REPLACE###,
	"name": ###REPLACE###,
	"projectLeader": ###REPLACE###,
	"endDate": ###REPLACE###,
	"budget": ###REPLACE###,
	"description": ###REPLACE###,
	"imageUrl": ###REPLACE###
}

### Test 05: Modify an existing projects 
### Positive test 
@proId = {{projects.response.body.0.projectId}} 
PUT {{BASE}}/projects/{{proId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"userId": ###REPLACE###,
	"name": ###REPLACE###,
	"projectLeader": ###REPLACE###,
	"endDate": ###REPLACE###,
	"budget": ###REPLACE###,
	"description": ###REPLACE###,
	"imageUrl": ###REPLACE###
}

### Check the modified projects

GET {{BASE}}/projects/{{newprojectsid}}
Content-Type: application/json 

### Test 06: Try to modify an existing projects without a session token
### Negative test
@proId = {{projects.response.body.0.projectId}}
PUT {{BASE}}/projects/{{proId}}
Content-Type: application/json

{
	"userId": ###REPLACE###,
	"name": ###REPLACE###,
	"projectLeader": ###REPLACE###,
	"endDate": ###REPLACE###,
	"budget": ###REPLACE###,
	"description": ###REPLACE###,
	"imageUrl": ###REPLACE###
}

### Test 07: Delete an existing projects
### Positive test

### Create a new projects, which we will delete
# @name createdproToDelete
POST {{BASE}}/projects
Content-Type: application/json
Token: {{token}}

{
	"userId": ###REPLACE###,
	"name": ###REPLACE###,
	"projectLeader": ###REPLACE###,
	"endDate": ###REPLACE###,
	"budget": ###REPLACE###,
	"description": ###REPLACE###,
	"imageUrl": ###REPLACE###
}

### Check the created projects
@prodelId = {{createdproToDelete.response.body.lastId}}
GET {{BASE}}/projects/{{prodelId}}
Content-Type: application/json 

### Delete the projects
DELETE {{BASE}}/projects/{{prodelId}}
Token: {{token}}

### Check the deleted projects
GET {{BASE}}/projects/{{prodelId}}
Content-Type: application/json 

### Test 08: Try to delete a projects without a session token
### Negative test
DELETE {{BASE}}/projects/{{proId}}

